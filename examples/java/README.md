# Java example

## Pre-requisites

* asdf

## Setup

```
asdf plugin add java
asdf install
```

## Run

```
NTFSVC_URL=(service URL) NTFSVC_APIKEY=(api key) ./gradlew run --args='(topic) "(message)"'
```
