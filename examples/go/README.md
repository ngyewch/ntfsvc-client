# Go example

## Pre-requisites

* asdf

## Setup

```
asdf plugin add golang
asdf install

go mod download
```

## Run

```
NTFSVC_URL=(service URL) NTFSVC_APIKEY=(api key) go run . "(topic)" "(message)"
```
